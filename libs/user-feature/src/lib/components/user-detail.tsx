'use client'; import {Card,CardContent,Typography} from '@mui/material'; import {useUser} from '../hooks/use-user'; export function UserDetail({userId}:{userId:string}){const {data,isLoading}=useUser(userId);if(isLoading)return <Typography>در حال بارگذاری…</Typography>;if(!data)return <Typography color="error">اطلاعات کاربر در دسترس نیست</Typography>;return <Card><CardContent><Typography variant="h5">{data.name}</Typography><Typography>{data.email}</Typography><Typography>{data.department}</Typography><Typography>{data.role === 'admin' ? 'مدیر' : 'کاربر'}</Typography></CardContent></Card>}
