'use client'; import {Card,CardContent,Typography} from '@mui/material'; import {useUser} from '../hooks/use-user'; export function UserDetail({userId}:{userId:string}){const {data,isLoading}=useUser(userId);if(isLoading)return <Typography>Loading…</Typography>;if(!data)return <Typography color="error">User unavailable</Typography>;return <Card><CardContent><Typography variant="h5">{data.name}</Typography><Typography>{data.email}</Typography><Typography>{data.department}</Typography><Typography>{data.role}</Typography></CardContent></Card>}
